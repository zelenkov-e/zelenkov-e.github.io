import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starterBlankApp',
  appName: 'blank-app',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
