const initialState = {
  buttonHide: "+",
  buttonLow: "-",
  buttonDel: "del",
  buttonShow: "show"
  //   optionElement: "option1"
};

let buttonReducer = (state = initialState) => state;

export default buttonReducer;
