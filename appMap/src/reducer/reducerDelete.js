// import { createReducer } from "redux-act";
// import { actionCreatorDelete } from "../action/action";

// // const defaultState = {
// //   // elemState: 0
// //   elemStateDelete: []
// // };

// // export const reducerDeleteApp = createReducer({}, defaultState);
// export const reducerDeleteApp = createReducer({}, []);

// reducerDeleteApp.on(actionCreatorDelete, (state, payload) => ({
//   ...state,
//   elemStateDelete: payload.idDelete
// }));
