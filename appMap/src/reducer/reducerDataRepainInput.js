// import { createReducer } from "redux-act";
// // import { actionCreatorRepaint } from "../action/action";

// const defaultState = {
//   // elemState: 0
//   elemState: []
// };

// // export const reducerState = createReducer({}, defaultState);

// // reducerState.on(actionCreator,actionCreatorDelete (state, payloadShow,payloadDelete) => ({
// //   ...state,
// //   elemState: payload.id
// // }));

// export const reducerDataRepainInput = function(state = defaultState, action) {
//   //   switch (action.type) {
//   // case "ELEM_RED":
//   //   return Object.assign({}, state, { elemState: action.payloadRepaintRed });
//   // case "ELEM_BLUE":
//   //   return Object.assign({}, state, { elemState: action.payloadRepaintBlue });
//   // case "ELEM_GREEN":
//   //   return Object.assign({}, state, {
//   //     elemState: action.payloadRepaintGreen
//   //   });
//   //   }
//   console.log(action.payloadRepaintRed);
//   //   return state;
// };
